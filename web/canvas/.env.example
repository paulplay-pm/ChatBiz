# ChatBiz Canvas UI — 开发环境变量
# 复制此文件到 .env

# Dev IAM token（vite-plugin-dev-iam.ts 本地注入）
VITE_DEV_TOKEN=dev-canvas-user

# 后端 API（Vite proxy => workflow-engine）
VITE_API_BASE=http://localhost:8001

# 默认 workspace（本地仅此一个）
VITE_WORKSPACE=default
