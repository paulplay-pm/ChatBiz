apiVersion: v1
kind: Service
metadata:
  name: chatbiz-audit-and-isolation
  namespace: chatbiz
  labels:
    app: chatbiz-audit-and-isolation
    component: data-isolation-gateway
spec:
  type: ClusterIP
  selector:
    app: chatbiz-audit-and-isolation
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
